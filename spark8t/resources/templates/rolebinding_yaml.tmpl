apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{resourcename}}
  namespace: {{namespace}}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{username}}-role
subjects:
- kind: ServiceAccount
  name: {{username}}
  namespace: {{namespace}}
